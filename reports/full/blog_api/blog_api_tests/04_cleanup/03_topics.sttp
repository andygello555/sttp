// Fetch all the topics so we can clean them up
topics = $GET("http://127.0.0.1:8000/api/topics");
// Test if we get the expected number of topics
test 0 + topics.content == 3;

// Delete each topic...
for _, topic in topics.content do
    topic_resp = $DELETE("http://127.0.0.1:8000/api/topics/%%" % topic.id);
    // Test if action has code 204 (No content)
    test topic_resp.code == 204;
end;
